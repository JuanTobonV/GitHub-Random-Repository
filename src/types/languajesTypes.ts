export type ProgrammingLanguaje = {
    title: string,
    value: string,
}

export type Repository = {
    full_name: string,
    name: string
    description: string,
    languaje: string,
    stars: number,
    forks: number,
}